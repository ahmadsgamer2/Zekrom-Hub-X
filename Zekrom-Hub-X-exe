local GetGame = loadstring(game:HttpGet("https://raw.githubusercontent.com/ahmadsgamer2/Zekrom-Hub-X/main/GameList"))()
function CheckingGame1()
    getgenv().Get =
        setmetatable(
        {},
        {
            __index = function(GetGame, B)
                return game:GetService(B)
            end
        }
    )
    for i, v in pairs(Games) do
        if i == game.PlaceId then
            loadstring(game:HttpGet(v))()
        end
    end
end
CheckingGame1()
